# Server
PORT=3000
NODE_ENV=development

# Database (Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=habit_tracker
DB_USER=postgres
DB_PASSWORD=your_db_password

# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRE=7d

# Client / Frontend
CLIENT_URL=http://localhost:5174
FRONTEND_URL=http://localhost:5174

# SMTP (optional) - if not set, development token is returned in response
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
